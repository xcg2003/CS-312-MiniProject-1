<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display Message</title>
</head>
<body>
    <h1><%= "Hello" %></h1>
    <div id="messages">
        <% messages.forEach(function(msg) { %>
            <p><strong><%= msg.username %>:</strong> <%= msg.message %></p>
        <% }); %>
    </div>
    <a href="/chat.html">Go back to chat</a>
</body>
</html>